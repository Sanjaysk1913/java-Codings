/*boolean and bitwise operstors
   ~ NOT           << left shift
   & AND           >> right shift
   | OR            >>> right shift zero fill
   ^ EXOR
*/
public class boolean_bitwise {
    public static void main(String[] args) {
        boolean a=true,b=false;
        System.out.println(a&b);
    }
    
}